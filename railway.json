{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "The WEB service: Next.js, the donor-facing pages, the checkout endpoint and Stripe's webhook. It holds no signing key. The worker service uses railway.worker.json instead -- set its 'Config as code' path to that file. See README.md, 'Deploying'.",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
